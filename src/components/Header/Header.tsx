import React from "react";

const Header: React.FC = () => {
  return (
    <header>
      <h1>
        Desarrollado con un toque de locura y una buena dosis de pasión, porque
        cada proyecto es una obra maestra en un lienzo de código.
      </h1>
    </header>
  );
};

export default Header;
